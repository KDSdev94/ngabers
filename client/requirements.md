## Packages
framer-motion | Smooth animations and transitions
usehooks-ts | Utility hooks like useDebounce for search
clsx | Conditional class names utility (often used with tailwind-merge)
tailwind-merge | Utility to merge tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
